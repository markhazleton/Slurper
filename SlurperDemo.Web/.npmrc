# Improve npm install speed
prefer-offline=true
audit=false

# Use specific registry (optional, uncomment if needed)
# registry=https://registry.npmjs.org/

# Save exact versions
save-exact=true

# Logging
loglevel=warn
